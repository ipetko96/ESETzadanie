{
  "name": "testeset",
  "version": "1.0.0",
  "description": "Simple REST API to test VM and check for Health Check.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ipet",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "express-healthcheck": "^0.1.0"
  }
}
